import OrgSelect from './OrgSelect';

const install = {
  install:function(Vue){

    //----------------------------------------- comps -------------------------------
    Vue.component('s-org-select',OrgSelect);
  }
}
export default install  ;
